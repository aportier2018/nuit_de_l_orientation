        <header>
            <nav class="navbar sticky-top navbar-expand-lg navbar-light bg-light">
                <a class="navbar-brand" href="{{ path('home') }}">Nuit de l'orientation</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01"
                    aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarColor01">
                    <ul class="navbar-nav ml-auto">
                        {% if not app.user %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('security_registration') }}">Inscription</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('security_login') }}">Connexion</a>
                            </li>
                        {% else %}
                            <li class="nav-item m">
                                <a class="nav-link" href="{{ path('search') }}">Nouvelle recherche</a>
                            </li>
                            <li class="nav-item dropdown">
                                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" id="accountDropdownLink">{{app.user.name}} {{app.user.firstname}}
                                </a>

                                <div class="dropdown-menu dropdown-menu-right" aria-labelledBy="accountDropdownLink">
                                    <a href="{{ path('user_compte', {id : app.user.id}) }}" class="dropdown-item">Mon compte</a>
                                    <a href="{{ path('security_profile') }}" class="dropdown-item">Modifier mon profil</a>
                                    <a href="{{ path('updatepassword') }}" class="dropdown-item">Modifier mon mot de passe</a>
                                    <div class="dropdown-divider"></div>
                                    <a class="nav-link" style="color: #f00;" href="{{ path('security_logout') }}">Déconnexion  <i class="fas fa-sign-in-alt"></i></a>
                                </div>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </nav>
        
        {% for label,messages in app.flashes %}
            <div class="messageflash text-center mx-auto ">
                <div class="alert alert-{{ label }}">
                    {% for message in messages %}
                        <p>{{ message | raw }}</p>
                    {% endfor %}
                </div>
            </div>
        {% endfor %}
        </header>